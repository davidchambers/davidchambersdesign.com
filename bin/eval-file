#!/usr/bin/env node

'use strict';

const {evaluateFile} = require ('../src/language.js');

process.argv
.slice (2)
.forEach (filename => {
  const e = evaluateFile ({}) (filename);
  if (e.isLeft) throw e.value;
});
