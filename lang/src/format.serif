export {
  list,
};

list strings = case strings of [
  []              -> "",
  [first]         -> first,
  [first, second] -> first <> " and " <> second,
  [...init, last] -> init & map (<> ",") & joinWith " " & (<> " and " <> last),
];
