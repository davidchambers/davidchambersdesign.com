import luxon from "luxon";

export default datetime;

datetime date time zone =
  (.fromFormat [date + " " + time + " (" + (Symbol.keyFor zone) + ")"]
               "yyyy-MM-dd HH:mm:ss (z)"
               #{"setZone" true}
               luxon.DateTime)
